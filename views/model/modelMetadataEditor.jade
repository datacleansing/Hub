extends base

block content
  .col-md-9
    form(
      action= URI_REPO + repoId + URI_MODELS + "/" + model.id
      method="PUT")
      .form-group
        label(for="nameInput") Name:
        input(
          type="text"
          class="form-control"
          id="nameInput"
          value= model.id
          readonly)

      include modelCommonForm.jade
      //button(type="submit" class="btn btn-default") Update
