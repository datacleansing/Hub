extends base

block scripts
  script(src='/scripts/repo.js')

block content
  p List models Here
  table(
    class="table table-hover"
    id='modelsTable'
  )
    each val in models
      tr
        td
          a(href= "/ui/models/" + val._id)
            text= val.metadata.name
