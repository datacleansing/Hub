extends base

block content
  .col-md-9
    table(
      class="table table-hover"
      id='modelsTable'
    )
      each val in models
        tr
          td
            a(href= URI_REPO + val.repository + URI_MODELS + "/" + val.id)
              text= val.id
