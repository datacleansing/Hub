extends base

block scripts
  script(src='/scripts/repo.js')

block content
  p List jobs here
  table(
    border='1'
    id='jobsTable'
  )
    each val in items
      tr
        td
          a(href= "/ui/jobs/" + val.key)
            text= "Job: " + val.name
