extends repo_base

block scripts
  script(src='/scripts/repo.js')

block content
  p List service Here
  p For every svc, show a link
  table(
    border='1'
    id='svcsTable'
  )
    each val in items
      tr
        td
          a(href= "/repo/svcs/" + val.key)
            text= val.name
